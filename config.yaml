model:
  provider: xai-oauth
  default: grok-4.3
fallback_providers:
  - provider: openai-codex
    model: gpt-5.6-sol
agent:
  max_turns: 90
approvals:
  mode: smart
display:
  busy_input_mode: queue
